# Raw geodata and bulk downloads — re-fetchable, ~165 MB and growing.
# Provenance lives in fetch.py, not in git.
data/
.venv/
__pycache__/
*.pyc
out/

# Run logs.
*.log

# Local browser/UAT evidence: screenshots and downloaded CSVs from review passes.
# Kept out of the image already; keep them out of the repository too.
artifacts/
.env
.env.*
!.env.example
.streamlit/secrets.toml
