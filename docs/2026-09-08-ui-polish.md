# UI polish — 8 September 2026

Superseded in part by `2026-09-08-visual-reference-followup.md`: the Analyse width constraint now belongs to a dedicated content container so navigation remains full width.

Follow-up to the full UI parity audit. Local changes only.

- Analyse now uses the reference's 1180 px outer width and 22/28/110 px top/side/bottom padding, scoped to the detail page.
- Screening heading line height and header bottom margin remove excess vertical space. Existing action column proportions remain, keeping both buttons on the same row.
- Sorting copy reflects built relative reserve, vacant developable floor area, or alternating parcel types. Ranking and calculation formulas are unchanged.

Verified through the Codex in-app browser on a separate local SQLite database: Analyse outer width measured 1180 px; final Screening filter starts at y=179.95 px at 1280 px viewport width; export/save buttons share y=135.95 px; mixed ranking label displays correctly.

Targeted regression: 41 tests passed (app and navigation). Python compilation and git diff whitespace checks passed. This follow-up does not establish complete visual parity, native mobile/browser coverage, or production verification. Not committed or pushed.
